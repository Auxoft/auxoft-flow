#!/bin/bash


local branch=$(git branch --no-color | grep '^\* ' | grep -v 'no branch' | sed 's/^* //g')

if [[ $branch == master ]]; then
  echo "Cannot commit on master"
  exit 1
fi

if [[ $branch == release/* ]]; then
  echo "Cannot commit on master"
  exit 1
fi

